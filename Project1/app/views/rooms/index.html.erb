<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

  <ul class="list-group">


    <% @rooms.each do |room| %>
        <li class="list-group-item">
          <%= room.room_id %>&nbsp&nbsp&nbsp&nbsp
          <%= room.building %>&nbsp&nbsp&nbsp&nbsp
          <%= link_to 'Show', room %>&nbsp&nbsp&nbsp&nbsp
          <%= link_to 'Edit', edit_room_path(room) %>&nbsp&nbsp&nbsp&nbsp
          <%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %>
      </li>
    <% end %>
  </ul>

</table>

<br>

<%  link_to 'New Room', new_room_path %>
