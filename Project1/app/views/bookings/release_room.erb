<h1>release the room you book</h1>

<table>
  <thead>
  <tr>
    <th>Room</th>
    <th>Date</th>
    <th>Starttime</th>
    <th>Endtime</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.room_id %></td>
        <td><%= booking.date.strftime('%Y-%m-%d') %></td>
        <td><%= booking.starttime %></td>
        <td><%= booking.endtime %></td>
<!--
        <%if booking.date.strftime('%Y-%m-%d')==Time.new.strftime('%Y-%m-%d')%>
            <td><%=link_to 'release', booking, method: :search, data: { confirm: "Releasing room?" }%></td>
        <% elsiff booking.date.strftime('%Y-%m-%d')>Time.new.strftime('%Y-%m-%d')%>
            <td><%= link_to 'cancle', booking, method: :cancel, data: { confirm: "Are you sure to cancel it?" } %></td>
        <% end %>
-->
        <td><%=link_to 'release', booking, method: :search, data: { confirm: "Releasing room?" }%></td>
        <td><%= link_to 'cancel', booking, method: :cancel, data: { confirm: "Are you sure to cancel it?" } %></td>
      </tr>
  <% end %>
  </tbody>
</table>
